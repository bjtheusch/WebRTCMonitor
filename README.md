# WebRTCMonitor
A chrome extension that monitors webrtc-internals and can notify the user when quality is poor.  It will store data locally in a sqlite database and can periodically log data relevant connection statistic information as well asIP address, username information and other relelvant info.
